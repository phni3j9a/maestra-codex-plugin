[project]
name = "maestra-codex-plugin"
version = "0.3.2"
description = "Context-isolated multi-agent orchestration plugin for Codex"
requires-python = ">=3.10"

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]
pythonpath = ["plugins/maestra/skills/run/scripts"]
