interface:
  display_name: "Maestra Main Gate"
  short_description: "Judge one Run at the semantic level"
  default_prompt: "Use $maestra:gate to evaluate the completed Run against user intent and architecture."
policy:
  allow_implicit_invocation: false
